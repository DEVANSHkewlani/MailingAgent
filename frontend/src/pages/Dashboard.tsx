/**
 * Dashboard Page — mounts the primary workspace application shell.
 */

import { AppShell } from '../components/app-shell'

export function Dashboard() {
  return <AppShell />
}

export default Dashboard
